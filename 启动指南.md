# SpeakNow — AI 英语口语训练系统 本地启动指南

## 环境要求

| 工具 | 版本要求 | 说明 |
|------|---------|------|
| Java | 17+ | 后端运行环境 |
| Node.js | 18+ | 前端运行环境 |
| MySQL | 8.0+ | 数据库，需先安装并启动 |
| Redis | 5.0+ | 缓存服务，需先安装并启动 |

---

## 第一步：启动 MySQL 并创建数据库

确保 MySQL 服务已启动，然后执行以下 SQL 创建数据库：

```sql
CREATE DATABASE IF NOT EXISTS english_speaking CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

> 默认连接配置：`localhost:3306`，用户名 `root`，密码 `123456`
> 如果您的 MySQL 密码不是 `123456`，请修改 `english-speaking-backend/es-server/src/main/resources/application.yml` 中 dev profile 的 `spring.datasource.password`。

---

## 第二步：启动 Redis

确保 Redis 服务已启动（默认端口 6379，无密码）。

验证 Redis 是否正常：

```bash
redis-cli ping
# 应返回 PONG
```

> Windows 用户可从 https://github.com/tporadowski/redis/releases 下载 Redis for Windows。

---

## 第三步：修改配置（如需要）

配置文件：`english-speaking-backend/es-server/src/main/resources/application.yml`

- **数据库密码**：如果不是 `123456`，修改 dev profile 下的 `spring.datasource.password`
- **Redis**：如果 Redis 有密码，修改 `spring.data.redis.password`
- **AI 服务**：dev 环境已预配置腾讯 ASR + DeepSeek LLM + EdgeTTS，开箱即用

---

## 第四步：启动后端

```bash
cd english-speaking-backend

# Windows
mvnw.cmd spring-boot:run -pl es-server

# macOS / Linux
./mvnw spring-boot:run -pl es-server
```

首次启动时 Flyway 会自动执行 V1-V30 迁移脚本，创建所有表并插入种子数据。

看到以下日志表示启动成功：

```
Started EsServerApplication in X.XXX seconds
```

后端运行在 `http://localhost:8088`，Swagger 文档：`http://localhost:8088/doc.html`

---

## 第五步：启动前端

```bash
cd english-speaking-frontend
npm install
npm run dev
```

前端运行在 `http://localhost:5173`，API 请求自动代理到 `localhost:8088`。

---

## 第六步：开始使用

打开浏览器访问 `http://localhost:5173`：

1. 点击「注册」创建个人账号
2. 登录后进入首页
3. **英语测评** — 完成 20 题定级测试，获取能力评估报告
4. **发音评测** — 选择句子录音，获取逐词发音打分
5. **话题陈述** — 围绕话题即兴表达，AI 评估流利度和准确性
6. **情景对话** — 选择场景与 AI 进行多轮对话练习
7. **角色扮演** — 模拟真实场景角色扮演
8. **学习进度** — 查看个人学习统计与进步轨迹
9. **语法纠错本** — 查看 AI 整理的语法错误与改进建议
10. **游戏化** — PK 对战、积分商城、徽章成就、排行榜
11. **社区** — 学习小组、挑战任务、同伴互评

### 管理后台

访问 `http://localhost:5173/admin` 使用预设账号登录：

| 角色 | 邮箱 | 说明 |
|------|------|------|
| 超级管理员 | admin@english-speaking.com | 全部管理权限 |
| 教师 | teacher@english-speaking.com | 班级/作业/学情管理 |
| 运维 | operator@english-speaking.com | 内容审核/工单/FAQ |

> 预设账号的初始密码请联系项目负责人获取，或直接注册新账号使用。

---

## 常见问题

**Q: MySQL 启动报错？**
A: Windows 以管理员身份运行终端，执行 `net start MySQL`。

**Q: 数据库连接失败？**
A: 检查 MySQL 是否已启动、密码是否正确、数据库 `english_speaking` 是否已创建。

**Q: Redis 连接失败？**
A: 确保 Redis 已启动（`redis-cli ping` 验证）。登录限流、锁定等功能依赖 Redis。

**Q: 端口被占用？**
A: 修改 `application.yml` 中 `server.port`，同步修改 `english-speaking-frontend/vite.config.ts` 中的代理目标端口。

**Q: 首次启动下载依赖慢？**
A: Maven Wrapper 首次运行会自动下载 Maven，项目依赖也会同步下载。如国内下载慢，可在 `english-speaking-backend/pom.xml` 中配置阿里云 Maven 镜像加速（已默认配置）。
