# 模块二：智能测评 V1.0 测试用例

> 版本：V1.0 MVP
> 测试范围：英语水平测评（20 题固定出题 + 三档定级），2 个 API，2 个前端页面
> 测试文档依据：PRD 需求文档 3.3 节 + 系统设计文档 2.2.2.2 节
> 生成日期：2026-06-26

---

## 目录

1. [功能测试 — 正常流程](#一功能测试--正常流程)
2. [功能测试 — 异常流程](#二功能测试--异常流程)
3. [功能测试 — 边界值测试](#三功能测试--边界值测试)
4. [API 契约检查](#四api-契约检查)
5. [安全合规检查](#五安全合规检查)
6. [前端集成测试检查清单](#六前端集成测试检查清单)

---

## 一、功能测试 — 正常流程

### 子模块 2.1：英语水平测评

#### TC-ASSESS-001：开始测评 — 成功获取 20 题

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-001 | 已登录用户点击"开始测评"，后端返回 20 道固定选择题（词汇/语法/阅读/听力各 5 题） | 用户已登录，Token 有效；题库中至少 20 条题目数据（4 类各 ≥5 条） | 1. 登录后进入首页<br/>2. 点击"开始测评"按钮<br/>3. 观察浏览器 Network 面板 | 1. 前端发送 GET /api/v1/assessment/questions?type=fixed<br/>2. 后端返回 HTTP 200，响应体 code=200<br/>3. data.questions 数组长度为 20<br/>4. 题目按类型分组：vocab 5 题、grammar 5 题、reading 5 题、listening 5 题<br/>5. 每题含 id、type、questionText、options(含 A/B/C/D)、audioUrl(null 或有效)、timeLimit=60<br/>6. 每题不含 correct_answer 字段<br/>7. data.assessmentId 为有效值 | PRD 3.3.1-2 流程图"按序出题"节点；设计文档 2.2.3.6 |

#### TC-ASSESS-002：逐题作答 — 展示题目 + 进度条 + 60s 倒计时

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-002 | 获取题目后，前端逐题展示题目卡片，含题目文本、4 个选项、进度条（第 N/20 题）、60s 倒计时 | TC-ASSESS-001 成功获取题目 | 1. 查看第一题展示<br/>2. 观察页面元素<br/>3. 点击一个选项进入下一题 | 1. 页面展示题目文本（questionText）<br/>2. 展示 4 个选项按钮（A/B/C/D）<br/>3. 页面顶部或侧边显示进度条，提示"第 1/20 题"<br/>4. 倒计时从 60 开始递减，每秒更新<br/>5. 点击选项后记录答案<br/>6. 0.2s 过渡动画后切换到下一题<br/>7. 进度条更新为"第 2/20 题"，倒计时重置为 60s | PRD 3.3.1-2 流程图 D1/D3 节点；设计文档 2.2.2.2.1 时序图 |

#### TC-ASSESS-003：倒计时最后 10 秒 — 红色脉冲动画

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-003 | 当某题倒计时进入最后 10 秒时，数字变红并脉冲闪烁 | 正在答题中，倒计时到 10 秒 | 1. 在一道题上等待倒计时从 60 降到 10<br/>2. 观察倒计时数字样式变化 | 1. 倒计时 >10s 时正常颜色（如黑色或灰色）<br/>2. 倒计时降至 10 时，数字变为红色<br/>3. 红色数字以脉冲动画闪烁（放大缩小或透明度变化）<br/>4. 提供视觉紧迫感 | PRD 3.3.1-2 流程图"倒计时60s"；设计文档 2.2.2.2.1 泳道图"最后10s变红闪烁" |

#### TC-ASSESS-004：60s 超时自动跳过 — 计 0 分并进入下一题

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-004 | 用户在 60s 内未点击任何选项，系统自动跳过该题，计 0 分，进入下一题 | 正在答题中 | 1. 进入一道题后不点击任何选项<br/>2. 等待倒计时归 0<br/>3. 观察页面变化 | 1. 倒计时从 60 逐步递减至 0<br/>2. 到达 0 时自动触发跳过<br/>3. 该题答案记为"超时"或"未作答"<br/>4. 页面自动切换到下一题<br/>5. 倒计时重置为 60s<br/>6. 进度条递增为下一题序号 | PRD 3.3.1-2 流程图 D4 节点"自动跳过计0分→下一题" |

#### TC-ASSESS-005：完成全部 20 题 — 提交答案并展示结果

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-005 | 用户完成全部 20 题后，系统自动提交答案，返回总分 + 四维分（词汇/语法/阅读/听力）+ 评级 | 已答完 20 题（含正常作答和超时跳过的题目） | 1. 完成第 20 题作答或超时<br/>2. 观察 Network 面板和页面变化 | 1. 前端发送 POST /api/v1/assessment/submit，请求体含 assessmentId 和 answers 数组<br/>2. 后端返回 HTTP 200，code=200<br/>3. data 含 totalScore（总分 0-100）、vocabScore/grammarScore/readingScore/listeningScore（四维分每题 5 分 × 各 5 题，各 0-25）<br/>4. data 含 level（beginner/intermediate/advanced）<br/>5. data 含 recordId（评测记录 ID）<br/>6. 页面跳转到 AssessmentResultPage 展示结果 | PRD 3.3.1-2 流程图 F 至 J 节点 |

#### TC-ASSESS-006：结果页 — 评级徽章展示（金/银/铜）

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-006 | 结果页根据用户的评级展示对应的徽章 | TC-ASSESS-005 提交成功 | 1. 观察结果页的评级徽章<br/>2. 验证不同等级对应不同徽章 | 1. 评级为 beginner（0-40 分）：展示铜色徽章<br/>2. 评级为 intermediate（41-70 分）：展示银色徽章<br/>3. 评级为 advanced（71-100 分）：展示金色徽章<br/>4. 徽章清晰可见，有不同的颜色和样式区分 | PRD 3.3.1-2 流程图 J 节点"展示结果页：总分+等级+四维得分" |

#### TC-ASSESS-007：结果页 — 总分渐增动画

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-007 | 结果页的总分数字从 0 平滑渐增到最终得分 | TC-ASSESS-005 提交成功 | 1. 进入结果页后观察总分数字<br/>2. 等待动画完成 | 1. 总分数字从 0 开始渐增<br/>2. 动画过程平滑（如 requestAnimationFrame 驱动）<br/>3. 最终停止在实际得分（如 75）<br/>4. 渐增时长约 1-2 秒 | 前端页面说明"总分渐增动画" |

#### TC-ASSESS-008：结果页 — 四维进度条（渐变）

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-008 | 结果页展示四维得分（词汇/语法/阅读/听力）的进度条，带渐变色 | TC-ASSESS-005 提交成功 | 1. 观察结果页四维得分区域<br/>2. 检查每个维度的进度条 | 1. 展示 vocabScore（词汇）、grammarScore（语法）、readingScore（阅读）、listeningScore（听力）<br/>2. 每个维度有一个进度条，满分为 25<br/>3. 进度条带有渐变色（如低分红、中分橙、高分绿）<br/>4. 进度条旁显示具体分数数字<br/>5. 维度标签清晰（中文或图标） | 前端页面说明"四维进度条（渐变）" |

#### TC-ASSESS-009：结果页 — 评语文案

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-009 | 结果页根据评级展示对应的鼓励/评语文案 | TC-ASSESS-005 提交成功 | 1. 观察结果页的评语文案<br/>2. 验证不同等级有不同文案 | 1. beginner：鼓励性文案，如"继续加油，从基础开始稳步提升！"<br/>2. intermediate：肯定性文案，如"表现不错，继续坚持练习！"<br/>3. advanced：赞扬性文案，如"非常优秀，你已经具备扎实的英语基础！"<br/>4. 文案不含技术术语或可能引起误解的表述 | PRD 3.3.1-2 流程图 J 节点 |

#### TC-ASSESS-010：结果页 — 更新 users.level 验证

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-010 | 测评完成后，后端更新 users 表中的 level 字段 | TC-ASSESS-005 提交成功 | 1. 完成测评并查看结果<br/>2. 直接查询 MySQL users 表该用户的 level 字段 | 1. users.level 已更新为测评结果对应的值<br/>2. 得分为 0-40 时 level='beginner'<br/>3. 得分为 41-70 时 level='intermediate'<br/>4. 得分为 71-100 时 level='advanced'<br/>5. 如果用户之前已有 level 且测评后等级变化，则覆盖旧值 | PRD 3.3.1-2 流程图 I 节点"更新users.level" |

#### TC-ASSESS-011：结果页 — 测评记录写入数据库验证

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-011 | 测评完成后，后端将完整记录写入 assessment_records 表 | TC-ASSESS-005 提交成功 | 1. 完成测评<br/>2. 查询 MySQL assessment_records 表该用户的记录 | 1. assessment_records 表新增一条记录<br/>2. user_id = 当前用户 ID<br/>3. assessment_type = 'fixed'<br/>4. total_score = 实际得分<br/>5. result_level = 评级（beginner/intermediate/advanced）<br/>6. answers_json 存储逐题答案信息<br/>7. created_at 为 UTC 时间戳 | PRD 3.3.1-2 流程图 J 节点；设计文档 3.2.1 assessment_records 表 |

#### TC-ASSESS-012：已测评用户重新测评 — 可再次进行

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-012 | 已经完成过一次测评的用户可以再次开始测评 | 用户已有一条 assessment_records 记录 | 1. 已测评用户登录后进入首页<br/>2. 点击"重新测评"或"开始测评"<br/>3. 完成全部 20 题 | 1. 系统允许再次进入测评<br/>2. 正常获取 20 道新题目（可从题库中随机或按序抽取）<br/>3. 完成提交后生成新的测评记录<br/>4. users.level 更新为最新评级<br/>5. assessment_records 表新增一条记录（非覆盖旧记录） | 设计文档 2.2.2.2.1 测评流程图 |

#### TC-ASSESS-013：测评过程中间歇性弱网 — 答案在前端暂存

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-ASSESS-013 | 测评过程中遇到弱网，前端保证每个答案在前端已记录，直到全部完成后一次性提交 | 正在答题中；网络不稳定 | 1. 开始答题<br/>2. 模拟答题过程中网络中断<br/>3. 继续点击选项作答<br/>4. 网络恢复后完成所有题目 | 1. 每道题答案在前端内存中暂存（如 Zustand store）<br/>2. 网络中断期间点击选项仍正常记录并切换到下一题<br/>3. 全部 20 题完成后前端等待网络恢复后提交<br/>4. 若超时无法提交，前端显示"网络不稳定，请重试"提示 | 前端页面说明 |

---

## 二、功能测试 — 异常流程

#### TC-ASSESS-101：题库不足 20 条 — 503 阻止进入测评

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-101 | 题库中题目总数不足 20 条（或某类型不足 5 条），系统阻止进入测评并返回友好提示 | 后端题库中题目总数 < 20 条，或 vocab/grammar/reading/listening 任一类型题目 < 5 条 | 1. GET /api/v1/assessment/questions?type=fixed 返回 HTTP 503<br/>2. 响应体 code=503，message="题库维护中，请稍后再试"<br/>3. 前端展示该提示，不进入答题页面<br/>4. 不生成 assessment_records 记录<br/>5. 不更新 users.level | PRD 3.3.1-2 流程图 B/E1 节点；设计文档 2.2.3.6 Error Codes；设计文档 6.3 友好文案 |

#### TC-ASSESS-102：未登录访问测评 API — 401

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-102 | 未登录用户（无 Token）直接请求 GET /api/v1/assessment/questions | 请求头不含有效 Authorization 或 Cookie（Token） | 1. 后端 Spring Security JWT Filter 拦截<br/>2. 返回 HTTP 401<br/>3. message="登录已过期，请重新登录"<br/>4. 不返回题目数据<br/>5. 前端跳转到 /login 页面 | 设计文档 2.2.2.2.1 时序图"Authentication: Bearer JWT" |

#### TC-ASSESS-103：Token 过期访问测评 API — 401

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-103 | 用户的 JWT Token 已过期（签发超过 7 天），尝试获取测评题目 | JWT Token 签发时间距今超过 7 天（exp 已过期） | 1. GET /api/v1/assessment/questions 返回 HTTP 401<br/>2. message="登录已过期，请重新登录"<br/>3. 前端 Axios 响应拦截器捕获 401<br/>4. 前端保存当前路径（/assessment）到 sessionStorage<br/>5. 前端跳转至 /login?redirect=/assessment | PRD 3.2.1-3 输出表"Token 过期"行 |

#### TC-ASSESS-104：提交空答案 — 422

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-104 | 用户提交的答案列表为空（所有 20 题均为超时或未作答） | answers 数组长度为 0 或不存在；或 answers 中每题的 selectedOption 均为 null | 1. 后端返回 HTTP 422<br/>2. message 为友好提示（如"未检测到有效作答记录"或"请至少完成一道题目"）<br/>3. 不写入 assessment_records<br/>4. 不更新 users.level<br/>5. 前端提示用户重新测评 | 设计文档 6.1 异常映射表 — 422 |

#### TC-ASSESS-105：提交 answers 数组长度不等于 20 — 400

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-105 | 用户提交的答案数组长度不为 20（如前端异常导致只记录了 18 题） | answers 数组长度 != 20 | 1. 后端返回 HTTP 400<br/>2. message 为友好提示（如"提交数据异常，请重新测评"）<br/>3. 不进行评分定级<br/>4. 不更新数据库 | 后端校验逻辑 |

#### TC-ASSESS-106：重复提交 — 幂等拒绝

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-106 | 同一用户短时间（如 5 秒）内对同一 assessmentId 重复提交 | 同一 user_id + 同一 assessmentId 在 5 秒内连续 POST 两次 | 1. 第二次请求返回 HTTP 409<br/>2. message 为友好提示（如"请勿重复提交"或"该测评已完成"）<br/>3. 仅第一次提交的结果被写入数据库<br/>4. 第二次不生成新记录 | 设计文档 2.2.2.2.1 泳道图"重复提交幂等校验" |

#### TC-ASSESS-107：测评中刷新或关闭页面 — 进度不保存

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-107 | 用户在答题过程中（如第 10 题）刷新页面或关闭浏览器标签页 | 用户正在进行测评，已作答 N 题（N<20） | 1. 前端触发 beforeunload 事件，弹出浏览器原生确认框："测评进度将丢失，确定离开吗？"<br/>2. 用户选择"离开"，刷新或关闭<br/>3. 重新进入测评页时，从头开始（第 1 题重新计时）<br/>4. 之前已作答的答案全部丢失<br/>5. 不产生测评记录 | PRD 3.3.1-2 流程图；设计文档 2.2.2.2.1 泳道图"中途刷新不保存" |

#### TC-ASSESS-108：未开始测评直接访问结果页 — 重定向

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-108 | 用户未完成测评，直接在浏览器地址栏输入结果页 URL | 用户登录但当前无未完成的测评记录；或 URL 中的 recordId 不属于当前用户 | 1. 如果前端有路由守卫：重定向到测评开始页或首页<br/>2. 如果直接访问 /assessment/result/xxx API：返回 HTTP 404（recordId 不存在）或 403（recordId 不属于当前用户）<br/>3. 前端展示友好提示"未找到测评记录" | 前端路由守卫设计 |

#### TC-ASSESS-109：提交请求体缺少必填字段 — 400

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-109 | 绕过前端直接向 POST /api/v1/assessment/submit 发送缺少 assessmentId 或 answers 字段的请求 | 请求体缺少 assessmentId 或 answers 字段 | 1. 后端 Spring Validation 校验失败<br/>2. 返回 HTTP 400<br/>3. message 为友好的字段校验提示（不含 "Field 'assessmentId' is required" 等技术术语）<br/>4. 不进行评分定级<br/>5. 不写入数据库 | 设计文档 6.1 异常映射表 — 400 |

#### TC-ASSESS-110：使用伪造/篡改的 Token 访问测评 API — 401

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-ASSESS-110 | 使用手工构造或篡改过的 JWT Token 访问测评接口 | 提供的 Token 签名不匹配或 Payload 被修改 | 1. 后端 JWT 验签失败<br/>2. 返回 HTTP 401<br/>3. 不返回题目数据或评分结果<br/>4. 不泄露任何系统信息 | 安全约束 |

---

## 三、功能测试 — 边界值测试

### 定级边界值

#### TC-ASSESS-201：得分 0 — 定级 beginner

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-201 | totalScore | 0 分（全部答错或全部超时） | 1. 用户被评定为 beginner<br/>2. 响应 data.level = 'beginner'<br/>3. users.level 更新为 'beginner'<br/>4. 展示铜色徽章<br/>5. 四维分均为 0 | PRD 3.3.1-2 流程图 G/H1 |

#### TC-ASSESS-202：得分 40 — 定级 beginner（上边界）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-202 | totalScore | 40 分（答对 8 题） | 1. 用户被评定为 beginner<br/>2. 响应 data.level = 'beginner'<br/>3. users.level 更新为 'beginner'<br/>4. 展示铜色徽章 | PRD 3.3.1-2 流程图 H1"0-40分→初级" |

#### TC-ASSESS-203：得分 41 — 定级 intermediate（下边界）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-203 | totalScore | 41 分（答对 9 题，其中 1 题得 1 分实际按 5 分制为答对 8.2 题... 实际上每题 5 分，41=8 题×5 + 某题 1 分，这不对。应理解为刚好答对足够题数达到 41 分。举例：答对 9 题=45 分，然后手动构造 41 分需要调整后端逻辑。从需求看 41 分即为 intermediate 的起始分） | 1. 用户被评定为 intermediate<br/>2. 响应 data.level = 'intermediate'<br/>3. users.level 更新为 'intermediate'<br/>4. 展示银色徽章 | PRD 3.3.1-2 流程图 H2"41-70分→中级" |

#### TC-ASSESS-204：得分 70 — 定级 intermediate（上边界）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-204 | totalScore | 70 分（答对 14 题） | 1. 用户被评定为 intermediate<br/>2. 响应 data.level = 'intermediate'<br/>3. users.level 更新为 'intermediate'<br/>4. 展示银色徽章 | PRD 3.3.1-2 流程图 H2"41-70分→中级" |

#### TC-ASSESS-205：得分 71 — 定级 advanced（下边界）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-205 | totalScore | 71 分（答对 15 题，其中某题可能部分得分。实际每题 5 分制，71=14 题×5+1 分，或 15 题×5-4。可以是答对 15 题=75 分后再模拟扣分） | 1. 用户被评定为 advanced<br/>2. 响应 data.level = 'advanced'<br/>3. users.level 更新为 'advanced'<br/>4. 展示金色徽章 | PRD 3.3.1-2 流程图 H3"71-100分→高级" |

#### TC-ASSESS-206：得分 100 — 定级 advanced（满分）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-206 | totalScore | 100 分（全部答对，20 题全对） | 1. 用户被评定为 advanced<br/>2. 响应 data.level = 'advanced'<br/>3. users.level 更新为 'advanced'<br/>4. 展示金色徽章<br/>5. 四维分均为满分 25 | PRD 3.3.1-2 流程图 H3"71-100分→高级" |

### 倒计时边界值

#### TC-ASSESS-207：倒计时 11 秒 — 正常颜色

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-207 | 倒计时显示 | 剩余 11 秒 | 1. 倒计时数字为正常颜色（黑色/灰色），非红色<br/>2. 无脉冲闪烁动画 | 设计文档 2.2.2.2.1 泳道图"倒计时状态" |

#### TC-ASSESS-208：倒计时 10 秒 — 变红脉冲

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-208 | 倒计时显示 | 剩余 10 秒 | 1. 倒计时数字变为红色<br/>2. 开始脉冲闪烁动画<br/>3. 提供视觉紧迫感 | 设计文档 2.2.2.2.1 泳道图"最后10s变红闪烁" |

#### TC-ASSESS-209：倒计时 1 秒 — 仍然红色脉冲

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-209 | 倒计时显示 | 剩余 1 秒 | 1. 倒计时数字仍为红色并持续脉冲闪烁<br/>2. 倒计时即将归零 | 设计文档 2.2.2.2.1 |

#### TC-ASSESS-210：倒计时 0 秒 — 自动跳过

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-210 | 倒计时显示 | 剩余 0 秒 | 1. 倒计时归零那一刻触发自动跳过<br/>2. 该题计 0 分<br/>3. 页面自动切换到下一题<br/>4. 倒计时重置为 60s | PRD 3.3.1-2 流程图 D4 节点 |

### 测评题数边界值

#### TC-ASSESS-211：答题第 1 题 — 不可返回上一题

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-211 | 第 1 题界面 | 第 1/20 题 | 1. 页面上不显示"上一题"按钮或该按钮置灰不可点击<br/>2. 仅可点击选项作答<br/>3. 进度条显示"第 1/20 题" | 设计文档 2.2.2.2.1 泳道图"不可返回修改已答题目" |

#### TC-ASSESS-212：答题第 20 题 — 提交而非下一题

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-212 | 第 20 题界面 | 第 20/20 题 | 1. 进度条显示"第 20/20 题"<br/>2. 点击选项后触发的不是"下一题"而是"提交"<br/>3. 或按钮文案变为"提交答案"<br/>4. 提交后跳转结果页 | 设计文档 2.2.2.2.1 时序图"20题全部完成?→是" |

### 超时边界值

#### TC-ASSESS-213：第 20 题超时 — 自动提交

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-213 | 第 20 题倒计时 | 第 20 题 60s 超时 | 1. 第 20 题倒计时归零时自动触发提交<br/>2. 该题计 0 分<br/>3. 自动计算总分和定级<br/>4. 跳转结果页 | PRD 3.3.1-2 流程图 D4/D5/F 节点 |

### 前端暂存答案边界值

#### TC-ASSESS-214：测评中连接中断又恢复 — 前端仍然持有完整答案

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-ASSESS-214 | 前端答案暂存 | 网络中断 30s 后恢复 | 1. 中断期间前端继续允许用户答题<br/>2. 前端内存中仍完整持有所有已答题目的答案<br/>3. 网络恢复后可成功提交<br/>4. 如果所有题在网络恢复前已完成，前端在网络恢复后自动提交 | 前端页面说明 |

---

## 四、API 契约检查

### 题目获取接口

| 检查项ID | 检查项 | 接口路径 | 检查内容 | 预期结果 |
|---------|--------|---------|---------|---------|
| CT-ASSESS-001 | 获取题目成功响应格式 | GET /api/v1/assessment/questions?type=fixed | 响应体 JSON 结构 | `{ "code": 200, "message": "success", "data": { "assessmentId": <number>, "questions": [ { "id": <number>, "type": "vocab"\|"grammar"\|"reading"\|"listening", "questionText": "<string>", "options": { "A": "<string>", "B": "<string>", "C": "<string>", "D": "<string>" }, "audioUrl": null\|"<string>", "timeLimit": 60 } ] }, "timestamp": "<ISO8601 UTC>" }` |
| CT-ASSESS-002 | 题目数量 | GET /api/v1/assessment/questions?type=fixed | data.questions 数组长度 | 固定为 20 题 |
| CT-ASSESS-003 | 题目类型分布 | GET /api/v1/assessment/questions?type=fixed | questions 按 type 分组计数 | vocab=5, grammar=5, reading=5, listening=5 |
| CT-ASSESS-004 | 题目排序 | GET /api/v1/assessment/questions?type=fixed | questions 数组顺序 | 按 vocab→grammar→reading→listening 类型顺序排列，同类型内按 sort_order 升序 |
| CT-ASSESS-005 | 题目时间限制 | GET /api/v1/assessment/questions?type=fixed | 每题 timeLimit 字段 | 每题的 timeLimit = 60 |
| CT-ASSESS-006 | correct_answer 白名单过滤 | GET /api/v1/assessment/questions?type=fixed | 响应体全文搜索 "correct_answer" / "correctAnswer" | **绝对不出现**。每道题仅包含 id/type/questionText/options/audioUrl/timeLimit，不含 correct_answer 字段 |
| CT-ASSESS-007 | 题目字段完整性 | GET /api/v1/assessment/questions?type=fixed | 每题的字段列表 | 必须包含：id(数字)、type(字符串)、questionText(字符串)、options(对象，含A/B/C/D)、audioUrl(null或字符串)、timeLimit(数字60) |
| CT-ASSESS-008 | assessmentId 有效性 | GET /api/v1/assessment/questions?type=fixed | data.assessmentId | 为正整数，用于后续提交时关联 |
| CT-ASSESS-009 | options 各选项非空 | GET /api/v1/assessment/questions?type=fixed | 每题 options 中 A/B/C/D | 4 个选项的值均为非空字符串 |
| CT-ASSESS-010 | 听力题 audioUrl 非 null | GET /api/v1/assessment/questions?type=fixed | type=listening 的题目 audioUrl | listening 类型题目的 audioUrl 应为有效 URL 字符串（非 null），指向预生成的听力音频文件 |

### 答案提交接口

| 检查项ID | 检查项 | 接口路径 | 检查内容 | 预期结果 |
|---------|--------|---------|---------|---------|
| CT-ASSESS-011 | 提交答案请求体格式 | POST /api/v1/assessment/submit | 请求体 Content-Type 和 JSON 结构 | Content-Type: application/json。请求体：`{ "assessmentId": <number>, "answers": [ { "questionId": <number>, "selectedOption": "A"\|"B"\|"C"\|"D"\|null } ] }`。answers 数组长度为 20 |
| CT-ASSESS-012 | 提交成功响应格式 | POST /api/v1/assessment/submit | 响应体 JSON 结构 | `{ "code": 200, "message": "success", "data": { "recordId": <number>, "totalScore": <number 0-100>, "vocabScore": <number 0-25>, "grammarScore": <number 0-25>, "readingScore": <number 0-25>, "listeningScore": <number 0-25>, "level": "beginner"\|"intermediate"\|"advanced" }, "timestamp": "<ISO8601 UTC>" }` |
| CT-ASSESS-013 | 响应字段类型校验 | POST /api/v1/assessment/submit | data 中各字段的 JavaScript 类型 | totalScore=number(0-100), vocabScore=number(0-25), grammarScore=number(0-25), readingScore=number(0-25), listeningScore=number(0-25), level=string(beginner/intermediate/advanced), recordId=number |
| CT-ASSESS-014 | 四维分之和等于总分 | POST /api/v1/assessment/submit | vocabScore+grammarScore+readingScore+listeningScore | 严格等于 totalScore（每题 5 分，每类 5 题满分 25，20 题满分 100） |
| CT-ASSESS-015 | 不传 assessmentId — 400 | POST /api/v1/assessment/submit | 请求体不含 assessmentId | 400 + 友好校验提示 |
| CT-ASSESS-016 | 不传 answers — 400 | POST /api/v1/assessment/submit | 请求体不含 answers | 400 + 友好校验提示 |
| CT-ASSESS-017 | answers 数组长度为 0 — 422 | POST /api/v1/assessment/submit | answers: [] | 422 + 友好提示 |
| CT-ASSESS-018 | answers 数组长度不为 20 — 400 | POST /api/v1/assessment/submit | answers 数组长度为 18 | 400 + 友好提示 |

### 错误码对照

| 检查项ID | 检查项 | 接口路径 | 触发方式 | 预期 HTTP 状态码和 message |
|---------|--------|---------|---------|--------------------------|
| CT-ASSESS-019 | 获取题目 401（未登录） | GET /api/v1/assessment/questions | 无 Token | 401 + "登录已过期，请重新登录" |
| CT-ASSESS-020 | 获取题目 401（Token过期） | GET /api/v1/assessment/questions | Token 超过 7 天 | 401 + "登录已过期，请重新登录" |
| CT-ASSESS-021 | 获取题目 503（题库不足） | GET /api/v1/assessment/questions?type=fixed | 题库不足 20 条 | 503 + "题库维护中，请稍后再试" |
| CT-ASSESS-022 | 提交答案 401（未登录） | POST /api/v1/assessment/submit | 无 Token | 401 + "登录已过期，请重新登录" |
| CT-ASSESS-023 | 提交答案 400（校验失败） | POST /api/v1/assessment/submit | 缺少必填字段 | 400 + 友好校验提示 |
| CT-ASSESS-024 | 提交答案 409（重复提交） | POST /api/v1/assessment/submit | 同一 assessmentId 5s 内二次提交 | 409 + 友好提示 |

---

## 五、安全合规检查

### correct_answer 字段白名单过滤

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-ASSESS-001 | GET /api/v1/assessment/questions 返回数据中不含 correct_answer | 调用 API 获取题目，全文搜索响应体查找 "correctAnswer" 和 "correct_answer" | 0 匹配。JSON 各级均不出现正确答案字段 |
| SC-ASSESS-002 | 使用浏览器 DevTools 检查 Network 响应 | 在测评页面打开 DevTools Network 面板，查看 GET questions 响应 | 响应体中每道题目仅含 id/type/questionText/options/audioUrl/timeLimit，无正确答案 |
| SC-ASSESS-003 | 检查前端代码中是否能在运行时获取正确答案 | 在前端浏览器控制台打印题目数据对象 | 题目数据中无 correct_answer 或类似字段 |

### 提交请求安全

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-ASSESS-004 | 用户在提交请求体中手动注入 correct_answer 字段是否被忽略 | 构造 POST /api/v1/assessment/submit 请求，在 answers 数组中每题的 JSON 里额外加入 "correctAnswer": "B" 字段 | 后端忽略请求体中的 correctAnswer 字段，评分仅依据后端数据库中存储的正确答案，不受请求体中的 "作弊" 字段影响 |
| SC-ASSESS-005 | 用户在提交请求体中传入与标准答案不同的 selectedOption，再额外传入 correct_answer 试图覆盖 | 构造请求，selectedOption 为 "A"，但注入 "correctAnswer": "A" | 后端评分使用数据库中的正确答案与 selectedOption 对比，不读取请求体中的 correctAnswer 字段。若实际正确答案为 "B"，该题仍判错 |

### Token 校验

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-ASSESS-006 | 无 Token 访问测评接口被拒绝 | 不带 Authorization 头请求 GET /assessment/questions | 返回 401，不泄露题目数据 |
| SC-ASSESS-007 | 伪造 Token 访问测评接口被拒绝 | 使用自构造的无签名 JWT 字符串 | 返回 401，不泄露题目数据 |
| SC-ASSESS-008 | Token 过期访问测评接口被拒绝 | 使用已过期的 Token | 返回 401，不泄露题目数据 |
| SC-ASSESS-009 | 不同用户不能访问他人测评记录 | 用户 A 的 Token 请求用户 B 的 assessment recordId | 返回 403 或 404（不应暴露他人数据是否存在） |

### 友好文案检查

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-ASSESS-010 | 所有测评 API 错误响应 message 不含技术术语 | 遍历所有测评接口的错误响应 | 不包含以下技术术语：confidence、HTTP 500、JSON parse error、timeout、SQLException、NullPointerException、correctAnswer、correct_answer、password_hash、BCrypt、JWT、token、stack trace、exception、RuntimeException、Spring、Hibernate、MyBatis 等 |
| SC-ASSESS-011 | 503 题库维护提示不含实现细节 | 触发题库不足场景，检查 message | 不含 "COUNT(*)"、"SELECT"、"database"、"table"、"rows" 等技术细节 |
| SC-ASSESS-012 | 400 校验失败不含技术术语 | 触发参数校验失败，检查 message | 不含 "validation failed"、"Field error"、"rejected value" 等 Spring Validation 技术术语 |
| SC-ASSESS-013 | 500 兜底不含堆栈信息 | 模拟服务端异常（如有），检查响应 | message="系统繁忙，请稍后再试"，不含堆栈跟踪或异常类名 |

### 输入安全

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-ASSESS-014 | assessmentId 参数防 SQL 注入 | 在 GET /questions 的 type 参数传入 `fixed' OR '1'='1` | 后端参数化查询，不产生 SQL 注入。返回正常处理或参数校验拒绝 |
| SC-ASSESS-015 | answers 数组中 questionId 防 SQL 注入 | selectedOption 传入 `A'; DROP TABLE--` | 后端参数化查询/类型校验，不产生 SQL 注入。正常返回错误或忽略无效选项 |
| SC-ASSESS-016 | 测评接口防 CSRF（生产环境） | 从第三方域发起 POST /api/v1/assessment/submit | 生产环境开启 CSRF 保护后，跨域请求被拒绝 |

---

## 六、前端集成测试检查清单

### 页面路由与渲染

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-ASSESS-001 | AssessmentPage 路由 /assessment | 已登录用户访问 /assessment | 渲染测评页面，展示第一题（先加载中状态，题目返回后展示） |
| INT-ASSESS-002 | /assessment 未登录重定向 | 未登录状态访问 /assessment | 跳转到 /login?redirect=/assessment |
| INT-ASSESS-003 | AssessmentResultPage 路由 /assessment/result | 完成测评后自动跳转 | 从 /assessment 提交成功后自动跳转到 /assessment/result |
| INT-ASSESS-004 | 直接访问 /assessment/result 重定向 | 未完成测评直接在地址栏输入 /assessment/result | 重定向到 /assessment 或首页，不展示空结果页 |

### 请求路径与 baseURL

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-ASSESS-005 | Axios baseURL 配置 | 检查前端 api/client.ts 或类似配置文件 | baseURL = `/api/v1` |
| INT-ASSESS-006 | 获取题目请求路径 | 检查浏览器 Network 面板 | GET `/api/v1/assessment/questions?type=fixed` |
| INT-ASSESS-007 | 提交答案请求路径 | 检查浏览器 Network 面板 | POST `/api/v1/assessment/submit` |

### 字段名对齐

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-ASSESS-008 | 题目对象字段名 | 对比 BE 返回的 QuestionVO 与 FE types/ 中定义 | 字段名一致且为驼峰：id、type、questionText、options、audioUrl、timeLimit |
| INT-ASSESS-009 | 提交答案请求体字段名 | 检查 POST /assessment/submit 请求体 JSON | answers 数组中每项字段名为驼峰：questionId、selectedOption |
| INT-ASSESS-010 | 提交答案响应 data 字段名 | 检查响应 JSON | 字段名为驼峰：totalScore、vocabScore、grammarScore、readingScore、listeningScore、level、recordId |
| INT-ASSESS-011 | type 枚举值一致 | 对比 BE 返回 vs FE 类型定义 | vocab / grammar / reading / listening（大小写一致） |
| INT-ASSESS-012 | level 枚举值一致 | 对比 BE 返回 vs FE 类型定义 | beginner / intermediate / advanced（大小写一致） |
| INT-ASSESS-013 | options 字段结构一致 | 检查题目 options 对象 | BE 返回 `{ "A": "...", "B": "...", "C": "...", "D": "..." }`，FE 按此结构解析渲染选项按钮 |

### 前端交互行为

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-ASSESS-014 | 点击选项过渡动画 | 点击某题选项后观察 | 0.2s 过渡动画（如淡入淡出或滑动），然后展示下一题。动画流畅不卡顿 |
| INT-ASSESS-015 | 点击选项后按钮禁用 | 点击某题选项后再次快速点击其他选项 | 点击后所有选项按钮立即禁用（disabled），防止同一题重复选择 |
| INT-ASSESS-016 | 倒计时 60s → 0s 实时更新 | 观察倒计时数字 | 每秒递减，从 60 到 1，无跳秒或停滞 |
| INT-ASSESS-017 | 倒计时 ≤10s 红色脉冲动画 | 倒计时 ≤10s 时观察 | 数字变红色，有脉冲动画（如 CSS animation scale/opacity），动画流畅 ≥30fps |
| INT-ASSESS-018 | 60s 超时自动跳下一题 | 倒计时归零时观察 | 自动切换到下一题，倒计时重置为 60s，进度条更新，无需用户操作 |
| INT-ASSESS-019 | beforeunload 拦截 | 答题中途（如第 5 题）点击浏览器刷新或关闭标签页 | 浏览器弹出原生确认框，提示测评进度将丢失 |
| INT-ASSESS-020 | 答题完成后 beforeunload 不拦截 | 提交答案后在第 20 题点击确定前和结果页 | 不再弹出确认框，可正常关闭或跳转 |
| INT-ASSESS-021 | 进度条显示 | 逐题检查进度条值 | 第 1 题显示 "1/20"，第 10 题显示 "10/20"，进度条填充比例对应递增 |
| INT-ASSESS-022 | 提交时 Loading 状态 | 完成 20 题后的提交过程 | 展示 Loading 动画或进度提示（如"正在评分中..."），按钮不可重复点击 |

### 结果页交互行为

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-ASSESS-023 | 结果页总分渐增动画 | 观察总分数字 | 从 0 平滑渐增到实际分数，动画时长约 1-2 秒，使用 easing 函数 |
| INT-ASSESS-024 | 结果页评级徽章 | 根据分数检查徽章 | beginner→铜色、intermediate→银色、advanced→金色。徽章有独特的视觉样式 |
| INT-ASSESS-025 | 结果页四维进度条渐变色 | 检查四个维度进度条颜色 | 低分段偏红/橙，中分段偏黄，高分段偏绿，进度条平滑填充 |
| INT-ASSESS-026 | 结果页"再来一次"按钮 | 点击结果页的"再次测评"或"重新测评"按钮 | 跳转回 /assessment，开始新的测评流程 |
| INT-ASSESS-027 | 结果页"返回首页"按钮 | 点击结果页的"返回首页"按钮 | 跳转到首页 / |

### Token 管理与错误处理

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-ASSESS-028 | Axios 请求拦截器自动携带 Token | 请求 GET /assessment/questions 时检查 Request Headers | Authorization 头或 Cookie 自动携带 Token |
| INT-ASSESS-029 | Axios 响应拦截器捕获 401 | 使用过期 Token 请求 /assessment/questions | 拦截器捕获 401，清除 Token，跳转 /login?redirect=/assessment |
| INT-ASSESS-030 | 题库不足提示 | 模拟题库不足场景，检查前端展示 | 展示"题库维护中，请稍后再试"，不进入答题页 |
| INT-ASSESS-031 | 提交失败重试 | 提交答案失败（如网络错误） | 前端展示"网络不稳定，请重试"提示，保留答案数据，用户可点击重试按钮重新提交 |

### 前端安全

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-ASSESS-032 | 前端代码中无 API Key/Secret | 全文搜索前端 src/ 目录 | 0 匹配结果 |
| INT-ASSESS-033 | 前端代码中无正确答案硬编码 | 全文搜索前端 src/ 目录中 "correct" "answer" 相关 | 不存在对正确答案的硬编码或映射表 |
| INT-ASSESS-034 | 前端不发送 correct_answer 字段 | 检查 POST /assessment/submit 请求体 | 请求体中每道题仅含 questionId 和 selectedOption，不含 correct_answer |

---

## 测试场景统计

| 分类 | 场景数 |
|------|--------|
| 功能测试 — 正常流程 | 13 |
| 功能测试 — 异常流程 | 10 |
| 功能测试 — 边界值测试 | 14 |
| API 契约检查 | 24 |
| 安全合规检查 | 16 |
| 前端集成测试检查清单 | 34 |
| **总计** | **111** |

---

*文档版本：V1.0*
*生成时间：2026-06-26*
*测试依据：PRD v2.0 3.3 节 + 系统设计文档 2.2.2.2 节 + 2.2.3.6 节 + 6 节出错处理设计*
