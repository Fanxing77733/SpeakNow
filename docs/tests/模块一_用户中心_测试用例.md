# 模块一：用户中心 V1.0 测试用例

> 版本：V1.0 MVP
> 测试范围：注册、登录、个人资料管理（4 个 API，3 个前端页面）
> 测试文档依据：PRD 需求文档 3.2 节 + 系统设计文档 2.2.2.1 节
> 生成日期：2026-06-26

---

## 目录

1. [功能测试 — 正常流程](#一功能测试--正常流程)
2. [功能测试 — 异常流程](#二功能测试--异常流程)
3. [功能测试 — 边界值测试](#三功能测试--边界值测试)
4. [API 契约检查](#四api-契约检查)
5. [安全合规检查](#五安全合规检查)
6. [前端集成测试检查清单](#六前端集成测试检查清单)

---

## 一、功能测试 — 正常流程

### 子模块 1.1：用户注册

#### TC-USER-001：邮箱注册成功 — 自动登录 — 跳转首页

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-001 | 用户使用合法邮箱完成注册，注册成功后自动登录并跳转首页 | 该邮箱未注册；IP 未超过注册频率限制 | 1. 访问 /register 页面<br/>2. 选择"邮箱"方式<br/>3. 输入合法邮箱（如 test@example.com）<br/>4. 输入合法密码（如 Abc12345，8-20位含字母数字）<br/>5. 再次输入相同密码确认<br/>6. 选择年龄（如 18 岁）<br/>7. 选择学习目标（如"日常交流"）<br/>8. 点击"注册"按钮 | 1. 前端表单校验通过，无红色错误提示<br/>2. 按钮显示加载中状态<br/>3. 后端返回 HTTP 200，响应体含 `code: 200`、`data.token`、`data.user`（含 nickname/email/age/goal/level）<br/>4. 前端显示"注册成功！"提示<br/>5. Token 写入 httpOnly Cookie（生产）或安全存储<br/>6. 页面自动跳转至首页 `/` | PRD 3.2.1-2 注册流程图 |

#### TC-USER-002：手机号注册成功 — 自动登录 — 跳转首页

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-002 | 用户使用合法手机号完成注册，注册成功后自动登录并跳转首页 | 该手机号未注册；IP 未超过注册频率限制 | 1. 访问 /register 页面<br/>2. 选择"手机号"方式<br/>3. 输入合法手机号（如 13800138000）<br/>4. 输入合法密码（如 Abc12345）<br/>5. 再次输入相同密码确认<br/>6. 选择年龄（如 25 岁）<br/>7. 选择学习目标（如"考试备考"）<br/>8. 点击"注册"按钮 | 1. 前端表单校验通过<br/>2. 后端返回 HTTP 200，响应体含 `code: 200`、`data.token`、`data.user`<br/>3. 前端显示"注册成功！"提示<br/>4. Token 正确存储<br/>5. 页面自动跳转至首页 `/` | PRD 3.2.1-2 |

#### TC-USER-003：注册成功 — 数据库记录验证

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-003 | 验证注册成功后数据库记录的正确性 | TC-USER-001 注册成功 | 1. 直接查询 MySQL users 表<br/>2. 检查新插入记录的各字段 | 1. `email` 字段 = 注册时输入的邮箱<br/>2. `password_hash` 字段不为明文（BCrypt 格式，以 `$2a$` 或 `$2b$` 开头）<br/>3. `password_hash` 字段中 strength=12（$2a$12$ 前缀）<br/>4. `age` = 注册时选择的年龄<br/>5. `goal` = 注册时选择的目标枚举值<br/>6. `nickname` 默认生成（如邮箱前缀或系统默认昵称）<br/>7. `level` 默认为 `beginner` 或 NULL<br/>8. `status` 为正常状态（如 `active`）<br/>9. `created_at` 和 `updated_at` 为 UTC 时间戳 | PRD 3.2.1-2 输出表 |

---

### 子模块 1.2：用户登录

#### TC-USER-004：邮箱+密码登录成功 — 跳转首页

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-004 | 已注册用户使用正确的邮箱和密码登录 | 已有通过 TC-USER-001 注册的账号；账号未被锁定 | 1. 访问 /login 页面<br/>2. 在账号输入框输入已注册邮箱<br/>3. 在密码输入框输入正确密码<br/>4. 点击"登录"按钮 | 1. 前端校验通过，无错误提示<br/>2. 按钮显示加载中状态<br/>3. 后端返回 HTTP 200，响应体含 `code: 200`、`data.token`、`data.user`<br/>4. Token 正确存储<br/>5. 页面跳转至首页 `/`<br/>6. 首页显示已登录状态（头像/昵称/导航栏变化） | PRD 3.2.1-3 登录流程图 |

#### TC-USER-005：手机号+密码登录成功

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-005 | 已注册用户使用正确的手机号和密码登录 | 已有通过 TC-USER-002 注册的账号；账号未被锁定 | 1. 访问 /login 页面<br/>2. 在账号输入框输入已注册手机号<br/>3. 在密码输入框输入正确密码<br/>4. 点击"登录"按钮 | 1. 后端返回 HTTP 200<br/>2. 成功获取 Token + 用户信息<br/>3. 页面跳转至首页 `/` | PRD 3.2.1-3 |

#### TC-USER-006：密码错误后重试正确密码 — 登录成功且错误计数重置

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-006 | 用户先输错一次密码，再输入正确密码登录成功，验证错误计数器被重置 | 已有注册账号 | 1. 使用正确账号 + 错误密码登录一次（收到 401）<br/>2. 使用正确账号 + 正确密码登录 | 1. 第一次返回 401 "密码错误，请重试"<br/>2. 第二次返回 200，登录成功<br/>3. Redis 中该用户的错误计数 Key 被删除（重置） | PRD 3.2.1-3 流程图"重置错误计数"节点 |

---

### 子模块 1.3：个人资料管理

#### TC-USER-007：已登录用户查看个人资料 — 数据脱敏展示

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-007 | 已登录用户进入个人资料页，查看信息并验证敏感数据脱敏 | 已登录，Token 有效；用户有完整资料（含手机号） | 1. 点击导航栏"我的"或访问 /profile 页面<br/>2. 观察页面加载过程<br/>3. 查看展示的个人资料信息 | 1. 页面加载时先展示骨架屏<br/>2. 骨架屏消失后展示真实数据<br/>3. 展示昵称（可编辑）<br/>4. 展示年龄（可编辑下拉选择器）<br/>5. 展示学习目标（可编辑下拉选择器）<br/>6. 展示当前水平等级（只读）<br/>7. 手机号以脱敏形式展示（如 138\*\*\*\*8000，中间4位隐藏）<br/>8. 邮箱完整展示<br/>9. 页面底部可见"退出登录"按钮 | PRD 3.2.2-3(1) |

#### TC-USER-008：编辑昵称 — 保存成功

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-008 | 已登录用户修改昵称并保存 | 已登录；Token 有效 | 1. 在 /profile 页面点击昵称编辑按钮<br/>2. 将昵称修改为"小明学英语"<br/>3. 点击"保存"按钮 | 1. 前端校验通过（2-20字符）<br/>2. 发送 PUT /api/v1/user/profile 请求<br/>3. 后端返回 HTTP 200<br/>4. 页面显示"更新成功"提示<br/>5. 昵称更新为"小明学英语"<br/>6. 数据库 users 表 nickname 字段已更新 | PRD 3.2.2-3(2) |

#### TC-USER-009：修改年龄 — 提示影响推荐

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-009 | 已登录用户修改年龄，系统提示可能影响推荐 | 已登录；用户当前年龄为 18 岁 | 1. 在 /profile 页面修改年龄从 18 改为 25<br/>2. 点击"保存"按钮 | 1. 前端弹出提示："修改年龄可能影响内容推荐，确定保存吗？"<br/>2. 用户确认后发送 PUT 请求<br/>3. 后端返回 HTTP 200<br/>4. 年龄更新为 25<br/>5. 后端标记推荐重算标志位 | PRD 3.2.2-3(4) |

#### TC-USER-010：修改学习目标 — 提示影响推荐

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-010 | 已登录用户修改学习目标，系统提示可能影响推荐 | 已登录；用户当前目标为 daily（日常交流） | 1. 在 /profile 页面修改学习目标从"日常交流"改为"考试备考"<br/>2. 点击"保存"按钮 | 1. 前端弹出提示："修改学习目标可能影响内容推荐，确定保存吗？"<br/>2. 用户确认后发送 PUT 请求<br/>3. 后端返回 HTTP 200<br/>4. 学习目标更新为 exam<br/>5. 后端标记推荐重算标志位 | PRD 3.2.2-3(4) |

#### TC-USER-011：修改学习目标为"商务英语"

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-011 | 已登录用户将学习目标切换为商务英语 | 已登录；当前目标为 daily 或 exam | 1. 在 /profile 页面修改学习目标为"商务英语"<br/>2. 确认提示后保存 | 1. 弹出推荐影响提示<br/>2. 确认后保存成功<br/>3. 学习目标更新为 business<br/>4. 数据库 goal 字段值 = 'business' | PRD 3.2.2-3(4) |

#### TC-USER-012：查看资料时不修改直接返回

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-012 | 用户进入个人资料页仅查看，不做修改 | 已登录 | 1. 进入 /profile 页面<br/>2. 查看资料后点击返回或导航到其他页面 | 1. 资料正确展示<br/>2. 不发送 PUT 请求<br/>3. 数据库中数据不变 | — |

#### TC-USER-013：退出登录确认弹窗

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-013 | 用户点击退出登录，确认后退出 | 已登录 | 1. 在 /profile 页面点击"退出登录"按钮<br/>2. 弹窗出现"确定退出登录？"<br/>3. 点击"确定" | 1. 弹出确认弹窗，含"取消"和"确定"两个按钮<br/>2. 点击"确定"后 Token 被清除（Cookie/Storage）<br/>3. 页面跳转至 /login<br/>4. 再次访问 /profile 时被重定向到 /login | PRD 3.2.2 + 前端页面说明 |

#### TC-USER-014：退出登录弹窗 — 取消操作

| 场景ID | 场景描述 | 前置条件 | 操作步骤 | 预期结果 | 对应AC |
|--------|---------|---------|---------|---------|--------|
| TC-USER-014 | 用户点击退出后选择取消 | 已登录 | 1. 点击"退出登录"<br/>2. 弹窗出现后点击"取消" | 1. 弹窗关闭<br/>2. 仍停留在 /profile 页面<br/>3. Token 未被清除<br/>4. 用户仍为登录状态 | — |

---

## 二、功能测试 — 异常流程

### 注册异常

#### TC-USER-101：重复邮箱注册 — 409

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-101 | 使用已注册的邮箱再次注册 | 该邮箱已存在一条 users 表记录 | 1. 后端返回 HTTP 409<br/>2. 响应体 `code: 409`<br/>3. `message` = "该邮箱/手机号已注册，请直接登录"<br/>4. 前端在表单字段下方显示该错误提示<br/>5. 不生成新用户记录<br/>6. 不返回 Token | PRD 3.2.1-2 注册流程图 E6 节点 |

#### TC-USER-102：重复手机号注册 — 409

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-102 | 使用已注册的手机号再次注册 | 该手机号已存在一条 users 表记录 | 1. 后端返回 HTTP 409<br/>2. `message` = "该邮箱/手机号已注册，请直接登录"<br/>3. 前端在表单字段下方显示该错误提示<br/>4. 不生成新用户记录 | PRD 3.2.1-2 |

#### TC-USER-103：同 IP 注册频率超限 — 429（第 4 次）

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-103 | 同一 IP 在 1 分钟内提交第 4 次注册请求 | 同 IP 在 60s 内已完成 3 次注册请求（无论成功与否） | 1. 后端返回 HTTP 429<br/>2. `message` = "操作过于频繁，请稍后再试"<br/>3. 前端以页面顶部 Toast 提示方式展示<br/>4. 不执行注册逻辑，不访问数据库 | PRD 3.2.1-2 注册流程图 E5 节点 |

#### TC-USER-104：前端校验 — 邮箱格式不合法

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-104 | 用户输入不合法的邮箱格式 | 输入邮箱不符合 RFC 5322 标准（如 "notanemail"、"a@b"、"@example.com"） | 1. 前端实时校验不通过<br/>2. 邮箱输入框下方显示红色提示"请输入正确的邮箱格式"<br/>3. "注册"按钮置灰或点击后不发送请求<br/>4. 不产生后端请求 | PRD 3.2.1-2 输入表 |

#### TC-USER-105：前端校验 — 手机号格式不合法

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-105 | 用户输入不合法的手机号格式 | 输入手机号不是 11 位或不是以 1 开头（如 "1234"、"20000138000"） | 1. 前端实时校验不通过<br/>2. 手机号输入框下方显示红色提示"请输入正确的手机号"<br/>3. 不发送后端请求 | PRD 3.2.1-2 输入表 |

#### TC-USER-106：前端校验 — 两次密码不一致

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-106 | 确认密码与密码输入不一致 | 密码输入 "Abc12345"，确认密码输入 "Abc12346" | 1. 前端实时校验不通过<br/>2. 确认密码输入框下方显示提示"两次密码输入不一致"<br/>3. 不发送后端请求 | 前端页面说明 |

#### TC-USER-107：前端校验 — 未选择学习目标

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-107 | 用户未选择学习目标直接提交注册 | 学习目标下拉框为默认未选状态 | 1. 前端校验不通过<br/>2. 学习目标选择器下方显示"请选择学习目标"<br/>3. 不发送后端请求 | PRD 3.2.1-2 注册流程图 E4 节点 |

#### TC-USER-108：后端校验 — 注册请求体缺少必填字段

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-108 | 绕过前端直接发送缺少必填字段的注册请求 | POST /api/v1/auth/register 请求体缺少 email/phone、password 字段 | 1. 后端 Spring Validation 校验失败<br/>2. 返回 HTTP 400<br/>3. `message` 为字段校验错误提示（友好文案）<br/>4. 不会出现类似 "Field 'email' is required" 等技术提示 | 设计文档 6.1 异常映射表 + 6.3 友好文案 |

---

### 登录异常

#### TC-USER-109：账号不存在 — 404

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-109 | 使用未注册的邮箱/手机号登录 | 输入的账号在 users 表中不存在 | 1. 后端返回 HTTP 404<br/>2. `message` = "账号不存在，请先注册"<br/>3. 前端在表单区域显示该提示<br/>4. Redis 不记录错误次数 | PRD 3.2.1-3 登录流程图 E4 节点 |

#### TC-USER-110：密码错误 — 401，显示剩余次数

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-110 | 账号存在但密码错误 | 账号存在；输入密码与 BCrypt hash 不匹配；错误次数 < 5 | 1. 后端返回 HTTP 401<br/>2. `message` = "密码错误，请重试"<br/>3. 前端可显示剩余尝试次数（如"密码错误，请重试（剩余 4 次）"）<br/>4. Redis 中该用户的 error:userId 计数 +1<br/>5. 不生成 Token | PRD 3.2.1-3 登录流程图 E6 节点 |

#### TC-USER-111：连续 5 次密码错误 — 403 锁定 30 分钟

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-111 | 同一账号连续输入 5 次错误密码 | 账号存在；已连续输错 4 次；第 5 次仍错误 | 1. 第 5 次错误后后端返回 HTTP 403（而非 401）<br/>2. `message` = "账号已被临时锁定，请30分钟后再试"<br/>3. Redis 中该用户锁定标记 TTL = 1800s（30分钟）<br/>4. 在锁定期间即使用正确密码也无法登录<br/>5. 锁定期间尝试登录直接返回 403（不走密码比对） | PRD 3.2.1-3 登录流程图 E5 节点 |

#### TC-USER-112：锁定期间使用正确密码 — 仍返回 403

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-112 | 账号锁定后，在 30 分钟内使用正确密码尝试登录 | TC-USER-111 触发锁定后，30 分钟内 | 1. 后端返回 HTTP 403<br/>2. `message` = "账号已被临时锁定，请30分钟后再试"<br/>3. 不进行密码比对<br/>4. 不更新错误计数器<br/>5. 锁定倒计时不重置 | PRD 3.2.1-3 登录流程图"账号已锁定?→是"分支 |

#### TC-USER-113：锁定 30 分钟到期后正确密码登录成功

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-113 | 账号锁定到期后使用正确密码登录成功 | TC-USER-111 触发锁定后已过 30 分钟，Redis 锁定 Key 已过期 | 1. 后端检查锁定状态 → 未锁定<br/>2. 密码比对成功<br/>3. 返回 HTTP 200 + Token + user<br/>4. 错误计数被重置<br/>5. 登录成功跳转首页 | PRD 3.2.1-3 流程图 C8 节点之后路径 |

#### TC-USER-114：同 IP 登录频率超限 — 429（第 11 次）

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-114 | 同一 IP 在 1 分钟内提交第 11 次登录请求 | 同 IP 在 60s 内已完成 10 次登录请求 | 1. 后端返回 HTTP 429<br/>2. `message` = "操作过于频繁，请稍后再试"<br/>3. 不执行登录逻辑 | PRD 3.2.1-3 登录流程图 E3 节点 |

#### TC-USER-115：前端校验 — 账号为空

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-115 | 不输入账号点击登录 | 账号输入框为空 | 1. 前端校验不通过<br/>2. 账号输入框下方显示"请输入账号"<br/>3. 不发送后端请求 | PRD 3.2.1-3 登录流程图 E1 节点 |

#### TC-USER-116：前端校验 — 密码为空

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-116 | 输入账号但不输入密码点击登录 | 密码输入框为空 | 1. 前端校验不通过<br/>2. 密码输入框下方显示"请输入密码"<br/>3. 不发送后端请求 | PRD 3.2.1-3 登录流程图 E2 节点 |

---

### 个人资料异常

#### TC-USER-117：未登录访问个人资料 API — 401

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-117 | 未携带 Token 或 Cookie 直接请求 GET /api/v1/user/profile | 请求头不含 Authorization/无有效 Token Cookie | 1. 后端 Spring Security JWT Filter 拦截<br/>2. 返回 HTTP 401<br/>3. `message` = "登录已过期，请重新登录"<br/>4. 不返回任何用户数据 | 设计文档 6.5.3 Token过期恢复 |

#### TC-USER-118：Token 过期访问个人资料 API — 401

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-118 | Token 已过期（签发超过 7 天），请求 GET /api/v1/user/profile | JWT Token 签发时间距今超过 7 天 | 1. 后端 JWT Filter 验签发现过期<br/>2. 返回 HTTP 401<br/>3. `message` = "登录已过期，请重新登录"<br/>4. 前端 Axios 拦截器捕获 401<br/>5. 前端保存当前页面路径到 sessionStorage<br/>6. 前端跳转至 /login 页面 | PRD 3.2.1-3 输出表"Token 过期"行；设计文档 6.5.3 |

#### TC-USER-119：使用伪造/篡改的 Token 访问 — 401

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-119 | 使用手工构造的 JWT Token 或篡改过的 Token 访问受保护接口 | 提供的 Token 签名不匹配或 Payload 被修改 | 1. 后端 JWT 验签失败<br/>2. 返回 HTTP 401<br/>3. 不返回用户数据<br/>4. 不泄露用户信息 | 安全约束 |

#### TC-USER-120：编辑资料 — 昵称校验失败

| 场景ID | 场景描述 | 触发条件 | 预期错误处理 | 对应AC |
|--------|---------|---------|-------------|--------|
| TC-USER-120 | 编辑资料时输入的昵称不符合规范（后端 Spring Validation 二次校验） | 昵称长度 < 2 或 > 20 字符，或纯数字 | 1. 前端实时校验不通过（优先拦截）<br/>2. 如绕过前端，后端返回 HTTP 400<br/>3. `message` 为友好的校验提示，不含 "validation failed" 等技术术语 | PRD 3.2.2-2 输入表 |

---

## 三、功能测试 — 边界值测试

### 密码边界值

#### TC-USER-201：密码 = 7 位 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-201 | password | 7 位（如 "Abc1234"） | 1. 前端校验不通过<br/>2. 提示"密码需8-20位，包含字母和数字"<br/>3. 不发送后端请求 | PRD 3.2.1-2 输入表 |

#### TC-USER-202：密码 = 8 位含字母数字 — 通过

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-202 | password | 8 位含字母+数字（如 "Abc12345"） | 1. 前端校验通过<br/>2. 允许提交到后端 | PRD 3.2.1-2 输入表 |

#### TC-USER-203：密码 = 20 位含字母数字 — 通过

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-203 | password | 20 位含字母+数字（如 "Abcdefgh1234567890ab"） | 1. 前端校验通过<br/>2. 允许提交到后端 | PRD 3.2.1-2 输入表 |

#### TC-USER-204：密码 = 21 位 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-204 | password | 21 位含字母+数字（如 "Abcdefgh1234567890abc"） | 1. 前端校验不通过<br/>2. 提示"密码需8-20位，包含字母和数字"<br/>3. 不发送后端请求 | PRD 3.2.1-2 输入表 |

#### TC-USER-205：密码 = 8 位纯数字 — 拒绝（缺少字母）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-205 | password | 8 位纯数字（如 "12345678"） | 1. 前端校验不通过<br/>2. 提示"密码需8-20位，包含字母和数字"<br/>3. 不发送后端请求 | PRD 3.2.1-2 输入表 |

#### TC-USER-206：密码 = 8 位纯字母 — 拒绝（缺少数字）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-206 | password | 8 位纯字母（如 "abcdefgh"） | 1. 前端校验不通过<br/>2. 提示"密码需8-20位，包含字母和数字"<br/>3. 不发送后端请求 | PRD 3.2.1-2 输入表 |

#### TC-USER-207：密码含特殊字符但含字母数字 — 通过

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-207 | password | 8 位含字母+数字+特殊字符（如 "Abc@1234"） | 1. 前端校验通过<br/>2. 允许提交到后端<br/>3. 后端 BCrypt 正常加密 | PRD 3.2.1-2 |

---

### 年龄边界值

#### TC-USER-208：年龄 = 5 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-208 | age | 5 岁 | 1. 前端选择器不提供 5 岁选项，或校验拦截<br/>2. 提示"请选择正确的年龄"（有效范围 6-99）<br/>3. 不提交到后端 | PRD 3.2.1-2 输入表 |

#### TC-USER-209：年龄 = 6 — 通过（下边界）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-209 | age | 6 岁 | 1. 前端校验通过<br/>2. 后端接收并存储 age=6<br/>3. 注册成功 | PRD 3.2.1-2 输入表 |

#### TC-USER-210：年龄 = 99 — 通过（上边界）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-210 | age | 99 岁 | 1. 前端校验通过<br/>2. 后端接收并存储 age=99<br/>3. 注册成功 | PRD 3.2.1-2 输入表 |

#### TC-USER-211：年龄 = 100 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-211 | age | 100 岁 | 1. 前端选择器不提供 100 岁选项，或校验拦截<br/>2. 提示"请选择正确的年龄"<br/>3. 不提交到后端 | PRD 3.2.1-2 输入表 |

---

### 昵称边界值

#### TC-USER-212：昵称 = 1 字符 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-212 | nickname | 1 个字符（如 "A"） | 1. 前端校验不通过<br/>2. 提示昵称需 2-20 字符<br/>3. 后端 Spring Validation 同样拒绝（返回 400） | PRD 3.2.2-2 输入表 |

#### TC-USER-213：昵称 = 2 字符 — 通过（下边界）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-213 | nickname | 2 个字符（如 "小明"） | 1. 前端校验通过<br/>2. 后端接收并更新<br/>3. 保存成功 | PRD 3.2.2-2 输入表 |

#### TC-USER-214：昵称 = 20 字符 — 通过（上边界）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-214 | nickname | 20 个字符 | 1. 前端校验通过<br/>2. 后端接收并更新<br/>3. 保存成功 | PRD 3.2.2-2 输入表 |

#### TC-USER-215：昵称 = 21 字符 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-215 | nickname | 21 个字符 | 1. 前端校验不通过<br/>2. 提示昵称需 2-20 字符<br/>3. 后端 Spring Validation 同样拒绝（返回 400） | PRD 3.2.2-2 输入表 |

#### TC-USER-216：昵称 = 纯数字（如 "12345"）— 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-216 | nickname | 纯数字如 "12345"（5位） | 1. 前端校验不通过<br/>2. 提示昵称不可为纯数字<br/>3. 后端同样拒绝 | PRD 3.2.2-2 输入表"不可纯数字" |

---

### 邮箱/手机号边界值

#### TC-USER-217：邮箱 = 101 字符 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-217 | email | 101 字符（超过 100 字符限制） | 1. 前端校验不通过<br/>2. 或后端返回 400<br/>3. 提示邮箱过长 | PRD 3.2.1-2 输入表 |

#### TC-USER-218：手机号 = 10 位 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-218 | phone | 10 位数字如 "1380013800" | 1. 前端校验不通过<br/>2. 提示"请输入正确的手机号" | PRD 3.2.1-2 输入表 |

#### TC-USER-219：手机号 = 12 位 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-219 | phone | 12 位数字如 "138001380000" | 1. 前端校验不通过<br/>2. 提示"请输入正确的手机号" | PRD 3.2.1-2 输入表 |

#### TC-USER-220：手机号首数字非 1 — 拒绝

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-220 | phone | 11 位但以 2 开头（如 "23800138000"） | 1. 前端校验不通过<br/>2. 提示"请输入正确的手机号" | PRD 3.2.1-2 输入表 |

---

### 错误计数边界值

#### TC-USER-221：错误第 4 次后第 5 次前（锁定临界值）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-221 | login error count | 第 4 次密码错误 | 1. 返回 401 "密码错误，请重试"（无剩余次数或显示 1）<br/>2. Redis error:userId 计数器 = 4<br/>3. 账号尚未锁定 | PRD 3.2.1-3 |

#### TC-USER-222：第 5 次错误立即锁定（锁定临界值）

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-222 | login error count | 第 5 次密码错误 | 1. 返回 403 "账号已被临时锁定，请30分钟后再试"<br/>2. Redis 锁定 Key TTL = 1800<br/>3. 后续 30 分钟内即使正确密码也无法登录 | PRD 3.2.1-3 |

---

### 注册频率边界值

#### TC-USER-223：第 3 次注册 — 通过；第 4 次 — 429

| 场景ID | 测试字段 | 边界值 | 预期行为 | 对应AC |
|--------|---------|--------|---------|--------|
| TC-USER-223 | register rate limit | 同 IP 60s 内第 3 次 vs 第 4 次 | 1. 第 3 次：正常处理（成功或 409 取决于数据）<br/>2. 第 4 次：返回 429 "操作过于频繁，请稍后再试" | PRD 3.2.1-2 |

---

## 四、API 契约检查

### 响应格式检查

| 检查项ID | 检查项 | 接口路径 | 检查内容 | 预期结果 |
|---------|--------|---------|---------|---------|
| CT-USER-001 | 注册成功响应格式 | POST /api/v1/auth/register | 响应体 JSON 结构 | `{ "code": 200, "message": "success" 或类似, "data": { "token": "<jwt_string>", "user": { "id": <number>, "email": "<string>", "nickname": "<string>", "age": <number>, "goal": "daily|exam|business", "level": "beginner|intermediate|advanced" } }, "timestamp": "<ISO8601 UTC>" }` |
| CT-USER-002 | 登录成功响应格式 | POST /api/v1/auth/login | 响应体 JSON 结构 | 与注册成功格式一致，包含 `data.token` 和 `data.user` 字段 |
| CT-USER-003 | 错误响应格式 | 全部接口 | 错误时的 JSON 结构 | 统一格式为 `{ "code": <http_status>, "message": "<友好文案>", "timestamp": "<ISO8601 UTC>" }` 或 `{ "code": <http_status>, "message": "<友好文案>", "requestId": "<uuid>", "timestamp": "<ISO8601 UTC>" }` |
| CT-USER-004 | 注册成功响应含 Token | POST /api/v1/auth/register | `data.token` 字段 | token 为非空字符串，可成功解析 JWT Payload 获取 userId |
| CT-USER-005 | 登录成功响应含 Token | POST /api/v1/auth/login | `data.token` 字段 | token 为非空字符串，JWT Payload 含 userId 和 exp（过期时间） |
| CT-USER-006 | Token 有效期 | POST /api/v1/auth/login | 解析 JWT exp 字段 | `exp - iat = 604800` 秒（7 天） |
| CT-USER-007 | 注册成功 user 对象字段 | POST /api/v1/auth/register | `data.user` 各字段 | 必须包含：id（数字）、email 或 phone（字符串）、nickname（字符串）、age（数字）、goal（daily/exam/business）、level（beginner 或 null） |
| CT-USER-008 | 登录成功 user 对象字段 | POST /api/v1/auth/login | `data.user` 各字段 | 与注册返回的 user 结构一致 |

### 个人资料接口检查

| 检查项ID | 检查项 | 接口路径 | 检查内容 | 预期结果 |
|---------|--------|---------|---------|---------|
| CT-USER-009 | 查看资料 phone 脱敏 | GET /api/v1/user/profile | 响应 `data.phone` 字段 | phone 不为 null 时，中间 4 位为星号（如 "138****8000"）。注意：phone 为 null 时（仅邮箱注册）正常返回 null，不报错 |
| CT-USER-010 | 查看资料不含 password_hash | GET /api/v1/user/profile | 响应体全文搜索 | 响应 JSON 中不包含 `password_hash` 或 `passwordHash` 字段 |
| CT-USER-011 | 查看资料响应字段完整列表 | GET /api/v1/user/profile | 响应 `data` 对象字段 | 必须包含：id、email、phone（脱敏后）、nickname、age、goal、level、createdAt 或 created_at |
| CT-USER-012 | 编辑资料请求格式 | PUT /api/v1/user/profile | 请求体 Content-Type | `application/json`，请求体含 `nickname`、`age`、`goal` |
| CT-USER-013 | 编辑资料成功响应 | PUT /api/v1/user/profile | 响应格式 | `{ "code": 200, "message": "更新成功", "data": null 或 { 更新后的user对象 }, "timestamp": "<ISO8601>" }` |
| CT-USER-014 | 编辑资料 — 未修改字段返回 | PUT /api/v1/user/profile | 所有字段与当前值相同 | 返回 200，message 为 "暂无变更" 或正常返回 |
| CT-USER-015 | 年龄/目标变更时提示标志 | PUT /api/v1/user/profile | 年龄或 goal 有变更时的响应 | 响应中可能包含 `data.recommendationChanged: true` 或 message 中提示"可能影响推荐内容" |

### 错误码对照检查

| 检查项ID | 检查项 | 接口路径 | 触发方式 | 预期 HTTP 状态码和 message |
|---------|--------|---------|---------|--------------------------|
| CT-USER-016 | 注册 409 | POST /api/v1/auth/register | 重复邮箱/手机号 | 409 + "该邮箱/手机号已注册，请直接登录" |
| CT-USER-017 | 注册 429 | POST /api/v1/auth/register | 同 IP 第 4 次/min | 429 + "操作过于频繁，请稍后再试" |
| CT-USER-018 | 注册 400 | POST /api/v1/auth/register | 缺少必填字段 / 密码格式不对 | 400 + 友好的字段校验提示 |
| CT-USER-019 | 登录 404 | POST /api/v1/auth/login | 账号不存在 | 404 + "账号不存在，请先注册" |
| CT-USER-020 | 登录 401 | POST /api/v1/auth/login | 密码错误（<5次） | 401 + "密码错误，请重试" |
| CT-USER-021 | 登录 403 | POST /api/v1/auth/login | 第5次密码错误 / 锁定期间登录 | 403 + "账号已被临时锁定，请30分钟后再试" |
| CT-USER-022 | 登录 429 | POST /api/v1/auth/login | 同 IP 第 11 次/min | 429 + "操作过于频繁，请稍后再试" |
| CT-USER-023 | 资料 401（未登录） | GET /api/v1/user/profile | 无 Token | 401 + "登录已过期，请重新登录" |
| CT-USER-024 | 资料 401（Token 过期） | GET /api/v1/user/profile | Token 超过 7 天 | 401 + "登录已过期，请重新登录" |
| CT-USER-025 | 资料 PUT 401 | PUT /api/v1/user/profile | 无 Token / Token 过期 | 401 + "登录已过期，请重新登录" |

---

## 五、安全合规检查

### 密码安全

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-USER-001 | 密码是否 BCrypt 加密存储（非明文） | 直接查询 MySQL users 表 password_hash 字段 | 所有 password_hash 值以 `$2a$12$` 或 `$2b$12$` 开头（BCrypt strength=12），全文无可读明文密码 |
| SC-USER-002 | 同一密码两次加密结果是否不同 | 注册两个账号使用相同密码，检查 password_hash | 两个 hash 值不同（BCrypt 随机盐） |
| SC-USER-003 | 密码长度是否在后端二次校验 | 绕过前端发送 7 位密码请求 | 后端 Spring Validation 拒绝，返回 400，不创建用户 |

### 敏感数据暴露检查

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-USER-004 | GET /user/profile 响应不含 password_hash | 调用 API 并全文搜索响应 | JSON 响应各级中均无 `password_hash`、`passwordHash` 等字段 |
| SC-USER-005 | POST /auth/login 响应不含 password_hash | 登录成功/失败响应全文搜索 | 响应中无密码哈希字段 |
| SC-USER-006 | POST /auth/register 响应不含 password_hash | 注册成功/失败响应全文搜索 | 响应中无密码哈希字段 |
| SC-USER-007 | 手机号脱敏展示 | 检查 GET /user/profile 中 phone 字段 | phone 中间 4 位为星号（如 "138****8000"） |
| SC-USER-008 | 数据库 password_hash 不可通过 API 暴露 | 尝试在 API 参数中注入字段名 "passwordHash" | API 不会因注入字段名而返回敏感字段 |

### 友好文案检查

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-USER-009 | 所有错误提示不含技术术语 | 遍历所有用户中心 API 的错误响应 message 字段 | 不包含：confidence、HTTP 500、JSON parse error、timeout、SQLException、NullPointerException、BCrypt、encoder、strength、matches、token、jwt 等技术术语 |
| SC-USER-010 | 403 锁定提示不含实现细节 | 触发账号锁定，检查 message | 不含 "Redis"、"SET"、"TTL"、"INCR" 等技术实现细节 |
| SC-USER-011 | 401 密码错误不含系统内部信息 | 检查密码错误响应 | 不含 "PasswordEncoder"、"BCrypt"、"hash not match" 等实现细节 |
| SC-USER-012 | 500 兜底文案为通用文案 | 触发未知异常（如有） | message = "系统繁忙，请稍后再试"，不含堆栈跟踪或异常类名 |

### Token 安全

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-USER-013 | Token 是否使用 httpOnly Cookie（生产） | 检查登录成功响应 Set-Cookie 头 | Cookie 设置 `HttpOnly`、`Secure`、`SameSite=Strict` 属性 |
| SC-USER-014 | Token 是否不在 URL / localStorage 中暴露（生产） | 检查前端代码是否有 `localStorage.setItem('token'` | 如为生产环境配置，Token 仅存于 httpOnly Cookie，无法通过 JS 读取 |
| SC-USER-015 | Token 过期后能否访问受保护接口 | 使用过期 Token 请求 GET /user/profile | 返回 401，不返回用户数据 |
| SC-USER-016 | 伪造 JWT（无签名）请求受保护接口 | 使用无签名的 JWT 格式字符串 | 返回 401，不返回用户数据 |

### 限流与锁定安全

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-USER-017 | 注册限流是否按 IP 生效 | 同一 IP 1 分钟内发起 4 次注册 | 第 4 次返回 429，后续短时间内的请求也被拒绝 |
| SC-USER-018 | 登录限流是否按 IP 生效 | 同一 IP 1 分钟内发起 11 次登录 | 第 11 次返回 429 |
| SC-USER-019 | 账号锁定是否生效 | 5 次错误密码后使用正确密码 | 返回 403，锁定 30 分钟 |
| SC-USER-020 | 锁定是否跨 IP 生效 | IP-A 触发锁定后，从 IP-B 使用正确密码登录同一账号 | 同样返回 403（锁定绑定账号，非 IP） |
| SC-USER-021 | 锁定到期后能否正常登录 | 等待 30 分钟后使用正确密码登录 | 登录成功，错误计数已重置 |

### 输入安全

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-USER-022 | 注册邮箱参数是否防 SQL 注入 | 在邮箱字段输入 `' OR '1'='1` | 后端参数化查询/MyBatis-Plus 预编译，不产生 SQL 注入。返回格式校验失败或正常处理（取决于是否符合邮箱格式） |
| SC-USER-023 | 注册昵称是否防 XSS | 在昵称输入 `<script>alert(1)</script>` | 后端/前端 HTML 转义，数据库中存原文，前端展示时转义不执行脚本 |
| SC-USER-024 | 密码字段是否截断或限制 | 传入超长密码（如 1000 字符） | 前端按 20 位截断或拒绝；后端 Spring Validation 限制最大长度 |

### CORS 与 CSP

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| SC-USER-025 | 生产环境 CORS 不为 `*` | 检查后端 CORS 配置 | Access-Control-Allow-Origin 为具体域名（非 `*`） |
| SC-USER-026 | 预检请求 OPTIONS 处理 | 发送 OPTIONS /api/v1/auth/login | 返回 200，含 CORS 头，不执行业务逻辑 |

---

## 六、前端集成测试检查清单

### 页面路由与渲染

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-USER-001 | LoginPage 路由 /login | 浏览器访问 /login | 渲染登录页（左右分栏布局），左侧品牌/插图区，右侧表单区 |
| INT-USER-002 | RegisterPage 路由 /register | 浏览器访问 /register | 渲染注册页，含邮箱/手机号切换、密码确认、年龄/目标选择器 |
| INT-USER-003 | ProfilePage 路由 /profile | 已登录状态访问 /profile | 渲染个人资料页，先骨架屏后真实数据 |
| INT-USER-004 | /profile 未登录重定向 | 未登录状态访问 /profile | 跳转到 /login，登录后回跳到 /profile |
| INT-USER-005 | 注册页"已有账号？去登录"链接 | 点击注册页底部链接 | 跳转至 /login 页面 |

### 请求路径与 baseURL

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-USER-006 | Axios baseURL 配置 | 检查前端 api/client.ts | baseURL = `/api/v1` |
| INT-USER-007 | 注册请求路径 | 检查浏览器 Network 面板 | POST `/api/v1/auth/register` |
| INT-USER-008 | 登录请求路径 | 检查浏览器 Network 面板 | POST `/api/v1/auth/login` |
| INT-USER-009 | 查看资料请求路径 | 检查浏览器 Network 面板 | GET `/api/v1/user/profile` |
| INT-USER-010 | 编辑资料请求路径 | 检查浏览器 Network 面板 | PUT `/api/v1/user/profile` |

### 字段名对齐

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-USER-011 | 注册请求体字段名 | 检查 POST /register 请求 JSON | 字段名为驼峰：email/phone、password、age、goal |
| INT-USER-012 | 注册响应 data.user 字段名 | 检查响应 JSON | 字段名为驼峰：id、email、phone、nickname、age、goal、level |
| INT-USER-013 | 登录响应 data.user 与注册一致 | 对比注册和登录返回的 user 对象 | 字段名、类型完全一致 |
| INT-USER-014 | 编辑资料请求体字段名 | 检查 PUT /user/profile 请求 JSON | nickname、age、goal（驼峰） |
| INT-USER-015 | goal 枚举值一致 | 对比 BE 返回 vs FE 类型定义 | daily / exam / business（大小写一致） |
| INT-USER-016 | level 枚举值一致 | 对比 BE 返回 vs FE 类型定义 | beginner / intermediate / advanced（大小写一致） |

### Token 管理与错误处理

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-USER-017 | Axios 请求拦截器自动携带 Token | 登录后发起 GET /user/profile，检查 Request Headers | Authorization 头或 Cookie 自动携带 Token |
| INT-USER-018 | Axios 响应拦截器捕获 401 | 使用过期 Token 请求 /user/profile | 拦截器捕获 401，清除 Token，跳转 /login |
| INT-USER-019 | 401 后保存回跳路径 | Token 过期时在 /profile 页面 | 跳转到 /login?redirect=/profile，登录成功后自动跳回 /profile |
| INT-USER-020 | 403 锁定错误前端展示 | 触发账号锁定后尝试登录 | 前端展示"账号已被临时锁定，请30分钟后再试"，不跳转 |
| INT-USER-021 | 429 限流错误前端展示 | 触发注册/登录频率限制 | 页面顶部 Toast 展示"操作过于频繁，请稍后再试" |
| INT-USER-022 | 弱网 / 超时前端处理 | 模拟网络延迟 > 10s | 前端展示"当前网络较慢"黄色提示条（连续 3 次超时触发） |

### 表单交互细节

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-USER-023 | 注册页邮箱/手机号切换 | 点击"手机号注册"切换 Tab | 输入框切换为手机号格式，校验规则同步切换 |
| INT-USER-024 | 登录页"记住密码"功能（如有） | 勾选后登录，退出再回来 | 账号被记住（填入输入框），密码需手动输入（安全考虑） |
| INT-USER-025 | 表单提交时按钮 Loading 态 | 点击注册/登录按钮 | 按钮变为 Loading 状态，不可重复点击 |
| INT-USER-026 | 后端返回错误后 Loading 恢复 | 注册失败（409） | 按钮恢复可点击状态，用户可重新修改提交 |
| INT-USER-027 | 页面切换时表单状态重置 | 在注册页输入一半后切换到登录页再切回 | 表单重置为空（或保留填写数据取决于产品设计，但提交后必须重置） |

### 前端安全

| 检查项ID | 检查内容 | 验证方法 | 预期结果 |
|---------|---------|---------|---------|
| INT-USER-028 | 前端代码中无 API Key/Secret | 全文搜索前端 src/ 目录 | 0 匹配结果（所有 AI API Key 仅在后端） |
| INT-USER-029 | 前端无 hardcoded 密码 | 搜索前端代码中的 password 常量 | 无硬编码密码 |
| INT-USER-030 | 前端错误提示不含技术信息 | 触发各类错误，检查前端展示的 Toast/提示文案 | 与技术无关的友好提示（由后端返回的 message 直接展示或前端映射） |

---

## 测试场景统计

| 分类 | 场景数 |
|------|--------|
| 功能测试 — 正常流程 | 14 |
| 功能测试 — 异常流程 | 20 |
| 功能测试 — 边界值测试 | 23 |
| API 契约检查 | 25 |
| 安全合规检查 | 26 |
| 前端集成测试检查清单 | 30 |
| **总计** | **138** |

---

*文档版本：V1.0*
*生成时间：2026-06-26*
*测试依据：PRD v2.0 3.2 节 + 系统设计文档 2.2.2.1 节 + 6 节出错处理设计*
